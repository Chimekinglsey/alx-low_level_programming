#include "main.h"
/**
 *  * _abs - main prints lowercase
 *
 *  @r: the parameter
 *
 *   * Return: A pointer to the resulting string
 *
 **/
int _abs(int r)

{
	if (r >= 0)
	{
		return (r);
	}

	else
	{
		return (r * -1);
	}
}
