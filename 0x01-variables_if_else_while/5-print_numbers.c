#include <stdio.h>
/**
 * main - Prints all single digits of base 10
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int A;

for (A = 0; A < 10; A++)
{
	printf("%d", A);
}

	printf('\n');
return (0);
}
