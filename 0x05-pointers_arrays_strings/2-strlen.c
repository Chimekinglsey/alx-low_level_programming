#include "main.h"
/**
 * _strlen - calculates the length of inputed string
 *
 * @s: takes the parameter and checks the string
 * Return: nothing
 **/
int _strlen(char *s)
{
	_putchar(strlen(s));
	return (0);
}
